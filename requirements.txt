# Install PyTorch for the target GPU first. On the g2-standard-8 L4 VM, for example:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#
# Flux2KleinPipeline is not in the 0.36 PyPI wheel. Pin Diffusers to the recorded SHA.
diffusers @ git+https://github.com/huggingface/diffusers.git@80c7ed262aeffbeb43ef13ae04baeb9b84515a69
accelerate>=1.4.0
transformers>=4.51.0
peft>=0.17.0
safetensors>=0.5.0
huggingface_hub>=0.30.0
Pillow>=10.4.0
PyYAML>=6.0.2
tqdm>=4.66.0
torchvision>=0.20.0
sentencepiece>=0.2.0
protobuf>=5.0.0
pytest>=8.0.0

# Optional, CUDA only: pip install bitsandbytes
# Enable with training.use_8bit_adam: true if 24 GB is still tight.
